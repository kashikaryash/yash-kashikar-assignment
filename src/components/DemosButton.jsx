const DemosButton = () => {
  return (
    <button
      className="fixed top-100 right-2 w-16 h-16 bg-black text-white font-bold text-xs flex items-center justify-center z-50 shadow-md hover:bg-gray-800 transition-all">
      DEMOS
    </button>
  );
};

export default DemosButton;
